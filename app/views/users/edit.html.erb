<h1><%= t('inPages.Users.forms.editing user') %></h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to t('inPages.Users.buttons.show user'), @user %> |
  <%= link_to t('inPages.Users.buttons.back to users'), users_path %>
</div>
