<p style="color: green"><%= notice %></p>

<%= render @team %>

<div>
  <%= link_to t('inPages.Teams.buttons.edit team'), edit_team_path(@team) %> |
  <%= link_to t('inPages.Teams.buttons.back to teams'), teams_path %>
  <%= button_to t('inPages.Teams.buttons.destroy team'), @team, method: :delete %>
</div>
