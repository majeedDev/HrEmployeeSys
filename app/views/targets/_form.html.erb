<%= form_with(model: target) do |form| %>
  <% if target.errors.any? %>
    <div style="color: red">
      <h2><%= t('globals.messages.error message', count: target.errors.count) %></h2>

      <ul>
        <% target.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label t('inPages.Targets.forms.title', default: 'Title'), style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label t('inPages.Targets.forms.description', default: 'Description'), style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label t('inPages.Targets.forms.start date', default: 'Start Date'), style: "display: block" %>
    <%= form.datetime_field :start_date %>
  </div>

  <div>
    <%= form.label t('inPages.Targets.forms.finish date', default: 'Finish Date'), style: "display: block" %>
    <%= form.datetime_field :finish_date %>
  </div>

  <div>
    <%= form.label t('inPages.Targets.forms.status', default: 'Status'), style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
